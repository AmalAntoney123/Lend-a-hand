import 'package:flutter/material.dart';

class ReportsPanel extends StatelessWidget {
  const ReportsPanel({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Reports - Coming Soon'),
    );
  }
}
